name: StegVerse AI Entity (ChatGPT)

on:
  pull_request:
    types: [opened, reopened, synchronize]
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read

jobs:
  ai_entity:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Prepare bounded PR context
        shell: bash
        run: |
          set -euo pipefail
          : > context.diff
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            git diff --unified=3 "${{ github.event.pull_request.base.sha }}" "${{ github.event.pull_request.head.sha }}" | head -c 24000 > context.diff || true
          fi

      - name: Run deterministic advisory fallback
        run: |
          python scripts/deterministic_advisory_fallback.py \
            --label "StegVerse AI Entity (ChatGPT)" \
            --context context.diff \
            --summary stegverse-ai-advisory.md \
            --receipt stegverse-ai-advisory.json

      - name: Publish job summary
        run: cat stegverse-ai-advisory.md >> "$GITHUB_STEP_SUMMARY"

      - name: Upload non-authorizing advisory receipt
        uses: actions/upload-artifact@v4
        with:
          name: stegverse-ai-deterministic-advisory-${{ github.run_id }}
          path: |
            stegverse-ai-advisory.md
            stegverse-ai-advisory.json
          if-no-files-found: error
