name: StegVerse AI Entity Bridge [CONTAINED — STEGVERSE PRIMARY / TV-TVC AUTHORITY]

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  contained:
    runs-on: ubuntu-latest
    steps:
      - name: Block legacy hosted-model bridge path
        shell: bash
        run: |
          set -euo pipefail
          echo "::error::This legacy bridge path is contained because it used a generic GitHub credential for hosted-model inference plus cross-repository context access and comment/issue mutation."
          echo "Replacement must be StegVerse-primary; any third-party/hosted inference is fallback-only under explicit TV/TVC route admission and may emit advisory evidence only."
          exit 1
