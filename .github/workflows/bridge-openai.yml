name: StegVerse AI Entity Bridge [CONTAINED — STEGVERSE PRIMARY / TV-TVC AUTHORITY]

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  contained:
    runs-on: ubuntu-latest
    steps:
      - name: Block legacy direct-provider bridge path
        shell: bash
        run: |
          set -euo pipefail
          echo "::error::This legacy bridge path is contained because it combined a generic GitHub credential, direct third-party provider credential processing, cross-repository context access, and comment/issue mutation."
          echo "Replacement must be StegVerse-primary, use third-party providers only as separately admitted fallback, process credentials only through TV/TVC, and preserve advisory/no-execution-authority semantics."
          exit 1
