name: autopatch [RETIRED — TV/TVC AUTHORITY REQUIRED]

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Block legacy direct-mutation path
        shell: bash
        run: |
          set -euo pipefail
          echo "::error::Legacy bridge autopatch is retired because it directly mutated repository state using GitHub/PAT credential paths outside the bounded TV/TVC authority model."
          echo "Any replacement must obtain mutation authority through the admitted TV/TVC execution boundary, preserve actor/token provenance, and emit non-secret evidence."
          exit 1
