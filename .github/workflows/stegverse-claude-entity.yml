name: StegVerse AI Entity Bridge [CONTAINED — STEGVERSE PRIMARY / TV-TVC AUTHORITY]

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  contained:
    runs-on: ubuntu-latest
    steps:
      - name: Block legacy direct-provider entity path
        shell: bash
        run: |
          set -euo pipefail
          echo "::error::This legacy entity workflow is contained because it combined generic GitHub and direct third-party provider credentials with cross-repository context access, comment/issue mutation, and hosted trace publication."
          echo "Replacement must be StegVerse-primary with third-party fallback only when explicitly required and admitted through TV/TVC; outputs remain advisory and must not acquire execution/admission authority."
          exit 1
