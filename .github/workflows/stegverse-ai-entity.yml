name: StegVerse AI Entity Bridge [CONTAINED — STEGVERSE PRIMARY / TV-TVC AUTHORITY]

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  contained:
    runs-on: ubuntu-latest
    steps:
      - name: Block legacy entity credential/mutation path
        shell: bash
        run: |
          set -euo pipefail
          echo "::error::This legacy entity workflow is contained because it combined generic GitHub and direct provider credentials with repository-dispatch input, cross-repository context access, and comment/issue mutation."
          echo "Replacement must use StegVerse-native execution first, explicit TV/TVC-admitted fallback only when required, and retain advisory/no-execution-authority semantics."
          exit 1
